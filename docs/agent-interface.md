# Agent Interface

The Hermes gateway is the main workbench entry point. It is the Hermes-branded layer over the upstream `openclaw` runtime.

It exposes:

- Hermes agent/workbench entry point
- visible filesystem panel
- GitHub integration panel
- project and workspace context
- safe auth status

It does not expose raw secret values.
