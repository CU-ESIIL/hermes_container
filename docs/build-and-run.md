# Build and Run

```bash
make build
make up
```

Stop with:

```bash
make down
```

Open a shell with:

```bash
make shell
```

Launch another isolated instance with:

```bash
scripts/start-instance.sh project-two 18790 8889 8091
```
